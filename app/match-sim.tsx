import React from "react";
import { ScrollView } from "react-native";
import MatchRadar from "../src/components/MatchRadar";

export default function MatchSim() {
  return (
    <ScrollView>
      <MatchRadar />
    </ScrollView>
  );
}
